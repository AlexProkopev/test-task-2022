import { Asap, Nunito } from "next/font/google";

export const nunitoSans = Nunito({
  variable: "--font-nunito-sans",
  subsets: ["latin"],
});

export const asapSans = Asap({
  variable: "--font-asap-sans",
  subsets: ["latin"],
});